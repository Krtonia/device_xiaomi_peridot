# Dolby
hal_client_domain(audioserver, hal_dms)

# Extcon access for audio codec and HDMI state
allow audioserver sysfs_extcon:dir search;
allow audioserver sysfs_extcon:file { getattr open read write };